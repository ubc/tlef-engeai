# Identity

One sentence: what this module instructs the model to do and when it applies.

# Instruction

Rules owned by this module only. Hand off detail owned elsewhere (do not paste other modules).

Use **CRITICAL:** and **IMPORTANT:** prefixes on lines that must not be overridden elsewhere in the same module.

Do not output internal tags like `<course_materials>` or `<struggle_topics>` in student-visible replies unless the module explicitly says otherwise.

# Examples

At least one good and one bad (or emit vs omit) example.

# Checklists

Pre-send verify boxes for this module only, using markdown task list syntax:

- [ ] First check
- [ ] Second check
