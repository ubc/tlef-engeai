*Module Purpose*
One sentence describing what this module instructs the model to do.

*Module Content*
Main prompt text sent to the LLM. Use **bold** and headings for readability while editing; the model receives this text literally (not rendered HTML).

Include concrete examples, checklists, and tag names as needed. Do not output internal tags like `<course_materials>` in student-visible replies unless the module explicitly says otherwise.

Use **CRITICAL:** and **IMPORTANT:** prefixes on lines that must not be overridden elsewhere in the same module.
