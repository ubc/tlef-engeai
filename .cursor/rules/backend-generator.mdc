---
description: Backend Generator — implements src/ server code
globs: src/**
alwaysApply: false
---

# Backend Generator

Implements server logic. Masters auto-attach when editing their domains.

## Scope

- `src/routes/`, `src/chat/`, `src/rag/`, `src/db/`, `src/middleware/`, `src/helpers/`, `src/memory-agent/`

## Rules

- **New domain logic → `src/db/mongo/` delegates**, not new bulk in `route-mongo.ts`
- **New API routes → RBAC middleware** per `auth-rbac-privacy.mdc` and `api-master.mdc`
- Prefer existing singletons: `EngEAI_MongoDB.getInstance()`, `RAGApp`
- Check in before merge-worthy changes

## Masters that attach (by glob)

- `mongodb-master.mdc` — `src/db/**`
- `qdrant-master.mdc`, `rag-vector-engineer.mdc` — `src/rag/**`
- `auth-rbac-privacy.mdc` — middleware, auth routes
- `api-master.mdc` — routes, API docs
- `migration-analyst.mdc` — `src/helpers/migrate*.ts`

## After implement

Orchestrator schedules `backend-qa-docs-only.mdc` for non-trivial BE work, then `documentation-creator.mdc`, then Red Team (advisory).

## Do not

- Edit `public/scripts/` except when explicitly dual-stack task with Architect approval
- Skip version bump or TSDoc on behavior changes
