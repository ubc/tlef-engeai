{
  "compilerOptions": {
    "target": "ES6",
    "module": "ES6",
    "lib": ["ES2016", "DOM", "DOM.Iterable"],
    "outDir": "./dist",
    "rootDir": "..",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true
  },
  "include": ["scripts/**/*"],
  "//exclude": "Jest specs are compiled by tsconfig.jest.json, never shipped to the browser. They may import from src/ (Node, zod, CommonJS), which this DOM/ES6 project cannot resolve.",
  "exclude": ["scripts/**/__tests__/**"]
}
